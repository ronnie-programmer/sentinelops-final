# ─── Backend API ─────────────────────────────────────────────────────────────
# Leave empty in development — Vite proxies /api → localhost:8000.
# In production (Vercel), set this to your Railway backend URL (no trailing slash).
# Example: VITE_API_URL=https://sentinelops-backend.railway.app
VITE_API_URL=
